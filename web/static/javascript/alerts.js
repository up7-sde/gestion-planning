/*var deleteButtons = document.querySelectorAll('#deleteButton');

for (var i = 0, len = deleteButtons.length; i < len; i++) {
    deleteButtons[i].addEventListener('click', (e) => {
        confirm('Vous êtes sur le point de supprimer une ressource. Etes vous sûr de vouloir continuer?') ? true : e.preventDefault();
    });
}

var modifyButtons = document.querySelectorAll('#modifyButton');

for (var i = 0, len = modifyButtons.length; i < len; i++) {
    modifyButtons[i].addEventListener('click', (e) => {
        confirm('Vous êtes sur le point de modifier une ressource. Etes vous sûr de vouloir continuer?') ? true : e.preventDefault();
    });
}*/